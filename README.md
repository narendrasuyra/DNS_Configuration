# DNS_Configuration
Configured a DNS server and HTTP web server in Cisco Packet Tracer, enabling domain-name-based website access. Demonstrates DNS resolution, IP addressing, router configuration, and network connectivity testing.
